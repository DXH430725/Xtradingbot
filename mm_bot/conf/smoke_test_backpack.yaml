general:
  log_config: mm_bot/conf/logging.yaml
  symbol: BTC_USDC_PERP

connectors:
  backpack:
    base_url: https://api.backpack.exchange
    ws_url: wss://ws.backpack.exchange
    keys_file: Backpack_key.txt
    broker_id: "1500"

strategy:
  name: smoke_test
  smoke_test:
    pause_between_tests_secs: 1.0
    connectors:
      backpack:
        symbol: BTC_USDC_PERP
        side: buy
        tracking_timeout_secs: 120.0
        tracking_interval_secs: 10.0
        settle_timeout_secs: 5.0
        market_timeout_secs: 30.0
        price_offset_ticks: 1
        cancel_wait_secs: 2.0
